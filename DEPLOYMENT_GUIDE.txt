🚀 DEPLOYMENT GUIDE - SECURE SWAGGER & PRODUCTION
=================================================

✅ Swagger is now secured with Basic Authentication in production!

═══════════════════════════════════════════════════════════════════
🔒 SWAGGER SECURITY
═══════════════════════════════════════════════════════════════════

Development (Local):
────────────────────
- Swagger is OPEN (no authentication required)
- Access: http://localhost:3000/api

Production (Deployed):
──────────────────────
- Swagger is PROTECTED with Basic Authentication
- Browser will prompt for username/password
- Credentials from environment variables

Default Credentials (CHANGE IN PRODUCTION!):
Username: admin
Password: changeme123

Set via environment variables:
SWAGGER_USER=your_username
SWAGGER_PASSWORD=your_secure_password

═══════════════════════════════════════════════════════════════════
📋 ENVIRONMENT VARIABLES
═══════════════════════════════════════════════════════════════════

Required for Production:
────────────────────────
NODE_ENV=production
PORT=3000
MONGODB_URI=mongodb+srv://user:pass@cluster.mongodb.net/dbname
JWT_SECRET=your-super-secret-256-bit-key
SWAGGER_USER=admin
SWAGGER_PASSWORD=your-strong-password-here

Optional:
─────────
REDIS_HOST=your-redis-host
REDIS_PORT=6379

═══════════════════════════════════════════════════════════════════
🚀 DEPLOYMENT STEPS
═══════════════════════════════════════════════════════════════════

Step 1: Update Environment Variables
─────────────────────────────────────
1. Copy .env.example to .env
2. Update all values (especially JWT_SECRET and SWAGGER_PASSWORD)
3. Set NODE_ENV=production

Step 2: Build the Project
──────────────────────────
npm run build

Expected: dist/ folder created with compiled code

Step 3: Test Production Build Locally
──────────────────────────────────────
NODE_ENV=production npm run start:prod

Test:
- Visit http://localhost:3000/api
- You should see Basic Auth prompt
- Enter SWAGGER_USER and SWAGGER_PASSWORD
- Access granted!

Step 4: Push to Git
───────────────────
git add .
git commit -m "feat: add secured swagger and production deployment"
git push origin main

Note: .env file is in .gitignore (not pushed)

═══════════════════════════════════════════════════════════════════
☁️ DEPLOYMENT PLATFORMS
═══════════════════════════════════════════════════════════════════

Option 1: Vercel (Already Configured!)
───────────────────────────────────────
1. Push to GitHub
2. Import project to Vercel
3. Set environment variables in Vercel dashboard
4. Deploy!

Vercel Environment Variables:
- NODE_ENV=production
- MONGODB_URI=mongodb+srv://...
- JWT_SECRET=...
- SWAGGER_USER=admin
- SWAGGER_PASSWORD=...

Option 2: Heroku
────────────────
1. Create Heroku app: heroku create
2. Add MongoDB addon: heroku addons:create mongodb:starter
3. Set config vars:
   heroku config:set NODE_ENV=production
   heroku config:set JWT_SECRET=your-secret
   heroku config:set SWAGGER_USER=admin
   heroku config:set SWAGGER_PASSWORD=your-password
4. Deploy: git push heroku main

Option 3: AWS EC2
─────────────────
1. Launch EC2 instance (Ubuntu)
2. Install Node.js and MongoDB
3. Clone repository
4. Create .env file with production values
5. Run: npm install && npm run build
6. Use PM2: pm2 start dist/main.js --name nestjs-backend
7. Setup nginx reverse proxy

Option 4: Docker
────────────────
Create Dockerfile:
```dockerfile
FROM node:20-alpine
WORKDIR /app
COPY package*.json ./
RUN npm ci --only=production
COPY . .
RUN npm run build
EXPOSE 3000
CMD ["npm", "run", "start:prod"]
```

Build and run:
docker build -t nestjs-backend .
docker run -p 3000:3000 \
  -e NODE_ENV=production \
  -e MONGODB_URI=... \
  -e JWT_SECRET=... \
  -e SWAGGER_USER=admin \
  -e SWAGGER_PASSWORD=... \
  nestjs-backend

Option 5: DigitalOcean App Platform
────────────────────────────────────
1. Connect GitHub repository
2. Set environment variables
3. Deploy automatically on git push

═══════════════════════════════════════════════════════════════════
🔐 SECURITY CHECKLIST
═══════════════════════════════════════════════════════════════════

Before Deploying to Production:
────────────────────────────────
✅ Set strong JWT_SECRET (256-bit recommended)
✅ Set strong SWAGGER_PASSWORD
✅ Set NODE_ENV=production
✅ Use HTTPS (SSL certificate)
✅ Use MongoDB Atlas or secure MongoDB instance
✅ Enable MongoDB authentication
✅ Set up firewall rules
✅ Enable CORS only for trusted domains
✅ Add rate limiting (optional but recommended)
✅ Set up error monitoring (Sentry)
✅ Set up logging (Winston/Morgan)
✅ Regular security updates (npm audit)

═══════════════════════════════════════════════════════════════════
🌐 ACCESSING SWAGGER IN PRODUCTION
═══════════════════════════════════════════════════════════════════

Step 1: Visit Swagger URL
──────────────────────────
https://your-domain.com/api

Step 2: Enter Basic Auth Credentials
─────────────────────────────────────
Browser will show popup:
Username: [enter SWAGGER_USER]
Password: [enter SWAGGER_PASSWORD]

Step 3: Authorize API Calls
────────────────────────────
1. Click "Authorize" button in Swagger
2. Register user: POST /auth/register
3. Login: POST /auth/login (get JWT token)
4. Enter: Bearer YOUR_JWT_TOKEN
5. Click "Authorize"
6. Now you can test all protected endpoints!

═══════════════════════════════════════════════════════════════════
📊 MONITORING & LOGS
═══════════════════════════════════════════════════════════════════

Production Monitoring:
──────────────────────
1. Check logs for Swagger access attempts
2. Monitor API usage
3. Track cache hit rates
4. Monitor database connections
5. Set up alerts for errors

Example PM2 logs:
pm2 logs nestjs-backend
pm2 monit

═══════════════════════════════════════════════════════════════════
🔄 UPDATING PRODUCTION
═══════════════════════════════════════════════════════════════════

1. Make changes locally
2. Run tests: npm run test && npm run test:e2e
3. Build: npm run build
4. Commit and push: git push origin main
5. Platform auto-deploys (Vercel, Heroku, etc.)

Or manually:
1. SSH to server
2. git pull
3. npm install
4. npm run build
5. pm2 restart nestjs-backend

═══════════════════════════════════════════════════════════════════
🚨 TROUBLESHOOTING
═══════════════════════════════════════════════════════════════════

Issue: Can't access Swagger in production
Solution: Check SWAGGER_USER and SWAGGER_PASSWORD are set correctly

Issue: Swagger accessible without password
Solution: Verify NODE_ENV=production is set

Issue: JWT tokens not working
Solution: Check JWT_SECRET is set and same across all instances

Issue: Database connection errors
Solution: Verify MONGODB_URI is correct and accessible

Issue: CORS errors
Solution: Update CORS configuration in main.ts

═══════════════════════════════════════════════════════════════════
📞 POST-DEPLOYMENT
═══════════════════════════════════════════════════════════════════

Share with team:
────────────────
API URL: https://your-domain.com
Swagger URL: https://your-domain.com/api
Swagger Username: [provided separately]
Swagger Password: [provided separately]

Documentation:
──────────────
- Share QUICK_START.txt
- Share API documentation
- Provide example requests
- Set up Postman collection

═══════════════════════════════════════════════════════════════════

🎉 Your API is now secure and ready for production deployment! 🚀

Remember to:
✅ Never commit .env files
✅ Use strong passwords
✅ Enable HTTPS
✅ Monitor your API
✅ Keep dependencies updated

